module.exports = {
  plugins: [
    require("tailwindcss"),
    require("autoprefixer"),
    // require("postcss-pxtorem")({
    //   rootValue: 16,
    //   minPixelValue: 2,
    //   propList: ['font', 'font-size', 'line-height', 'letter-spacing'],
    // }),
  ],
};
